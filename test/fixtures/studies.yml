DEFAULTS: &DEFAULTS
  topic: $LABEL
  name: $LABEL
  protocol: $LABEL
  agent: $LABEL
  mechanism: $LABEL
  side_effects: $LABEL
  administration: $LABEL
  randomization: $LABEL
  duration: $LABEL
  assessment_frequency: $LABEL
  interventions: $LABEL
  sponsor: $LABEL
  sponsor_contact: $LABEL
  cro_contact: $LABEL
  budget: $LABEL
  enrolled_or_committed: $LABEL
  comments: $LABEL
  published: false
  owner: $LABEL (User)
  creator: $LABEL (User)

one:
  <<: *DEFAULTS

two:
  <<: *DEFAULTS