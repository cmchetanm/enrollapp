DEFAULTS: &DEFAULTS
  study: $LABEL
  kind: <%= [CriterionType::EXCLUSION, CriterionType::INCLUSION].sample %>
  text: $LABEL
  owner: $LABEL (User)
  creator: $LABEL (User)

one:
  <<: *DEFAULTS

two:
  <<: *DEFAULTS